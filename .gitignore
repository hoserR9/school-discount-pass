node_modules/
data/
output/
*.pkpass
.DS_Store
pitch/~$*

# Certificates — placeholder certs are in git for development.
# When you get real Apple certs, set them as Railway environment
# variables (SIGNER_CERT, SIGNER_KEY, WWDR_CERT) instead of
# committing them to git.
